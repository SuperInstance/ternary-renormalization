# ternary-renormalization

****

## What's Inside

See `src/lib.rs` for the full API. All types are `#![forbid(unsafe_code)]` and `#![no_std]`.

## Quick Example

```rust
use ternary_renormalization::*;
// See tests in src/lib.rs for usage patterns
```

## The Deeper Truth

This crate explores a fundamental question: *what happens when you discretize a continuous concept to exactly three values?* The answer reveals hidden structure — algebraic invariants, exact computations, and surprising connections between seemingly unrelated domains.

## Install

```bash
cargo add ternary-renormalization
```

## License

MIT
